{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run app.exe",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/app.exe",
      "args": [],
      "cwd": "${workspaceFolder}",          // <-- THIS is the important line
      "MIMode": "gdb",
      "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
      "preLaunchTask": "Build app (miniaudio)"
    }
  ]
}
